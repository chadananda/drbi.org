---
// There is no login page — sign-in happens via the navbar account popover on any
// page. Kept only so old links/bookmarks land home with the popover open.
export const prerender = false;
return Astro.redirect('/?signin=1', 302);
---
