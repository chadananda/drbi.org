---
import Container from "@components/container.astro";
// import { Icon } from 'astro-icon/components'

// const { margin } = Astro.props;

// import brand from "@data/branding.json";
// import SiteLogo from "@components/siteLogo.astro";
// import {companyLinks, productLinks, resourceLinks} from "@data/siteLinks.json";
---
<!--
<footer class:list={[ "py-14 bg-slate-100 border-t border-slate-100 noprint", margin && "mt-10" ]}>
  <Container>
    <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-5 gap-8">
      <div class="sm:col-span-2 md:col-span-3 lg:col-span-2">
        <SiteLogo />
        <p class="mt-4 text-sm text-slate-700 max-w-xs"> {brand.description_long} </p>
        <div class="flex gap-3 mt-4 items-center">

          <a href="https://twitter.com/surjithctly"
            target="_blank"
            rel="noopener noreferrer"
            class="bg-slate-200 hover:bg-slate-300 rounded w-6 h-6 inline-flex items-center justify-center text-slate-500">
            <Icon name="bx:bxl-twitter" class="w-4 h-4" />
            <span class="sr-only">TwitterX</span></a>

            <a href="https://facebook.com/"
            target="_blank"
            rel="noopener noreferrer"
            class="bg-slate-200 hover:bg-slate-300 rounded w-6 h-6 inline-flex items-center justify-center text-slate-500">
            <Icon name="bx:bxl-facebook-circle" class="w-4 h-4" /><span
              class="sr-only">Facebook</span></a>

          <a href="https://linkedin.com/"
            target="_blank"
            rel="noopener noreferrer"
            class="bg-slate-200 hover:bg-slate-300 rounded w-6 h-6 inline-flex items-center justify-center text-slate-500">
            <Icon name="bx:bxl-linkedin-square" class="w-4 h-4" /><span
              class="sr-only">Linkedin</span></a>

        </div>
      </div>
    { !!companyLinks.length && (
      <div>
        <h3 class="font-medium text-sm text-slate-800">Company</h3>
        <div class="flex flex-col mt-3">
          {companyLinks.map((item) => (
              <a href={item.path || "#"}
                class="py-2 text-sm text-slate-600 hover:text-indigo-600">
                {item.title}
              </a>
          ))}
        </div>
      </div>
    )}

    { !!productLinks.length && (
      <div>
        <h3 class="font-medium text-sm text-slate-800">Product</h3>
        <div class="flex flex-col mt-3">
          {productLinks.map((item) => (
              <a href={item.path || "#"}
                class="py-2 text-sm text-slate-600 hover:text-indigo-600">
                {item.title}
              </a>
          ))}
        </div>
      </div>
      )}

   { !!resourceLinks.length && (
      <div>
        <h3 class="font-medium text-sm text-slate-800">Resources</h3>
        <div class="flex flex-col mt-3">
          {resourceLinks.map((item) => (
              <a
                href={item.path || "#"}
                class="py-2 text-sm text-slate-600 hover:text-indigo-600">
                {item.title}
              </a>
          ))}
        </div>
      </div>
    )}

    </div>
  </Container>

  <div class="mt-14 pt-8 px-5 border-t border-slate-200">
    <Container>
      <p class="text-center text-sm text-slate-600 [text-wrap:balance]">
        Copyright © {new Date().getFullYear()} {brand.site} &mdash; All rights reserved.
      </p>
      <p class="text-center text-xs text-slate-600 mt-2">
        <a href="/terms" class="hover:text-indigo-500"> Terms</a> &middot;
        <a href="/privacy" class="hover:text-indigo-500"> Privacy</a> &middot;
        <a href={brand.vercel_project_dashboard} target="_blank" class="hover:text-indigo-500"> Vercel Dashboard</a>
      </p>
    </Container>
  </div>

</footer> -->
