---
description: Turn an idea into a BDD .feature spec — no implementation
---
Turn the idea below into a Gherkin feature. Do NOT implement — spec only.

$ARGUMENTS

Write ONE `.feature` file: a `Feature:` line + 2-4 `Scenario:` blocks (concrete, testable Given/When/Then). Ask AT MOST 1 clarifying question, and only if genuinely blocked; otherwise state assumptions and proceed. Save to `tests/features/<slug>.feature`. Report the path + a one-line summary of each scenario. No step definitions, no code.
